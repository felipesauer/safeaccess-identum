# Generated by release-please — reformatting them only creates churn, since
# the next release rewrites them in its own style.
CHANGELOG.md
packages/*/CHANGELOG.md
.release-please-manifest.json

# Infection reads json5; Prettier rewrites the quoting style, which would
# fork this file away from its twin in safeaccess-inline.
packages/php/infection.json5
